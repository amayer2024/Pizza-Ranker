# Optional: shared cloud database (all three roommates see the same rankings)
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Optional: one shared PIN so a public URL cannot be edited by strangers
HOUSEHOLD_PIN=

# Optional: Google Places Text Search (New). Without this, Madrid search uses OpenStreetMap.
GOOGLE_PLACES_API_KEY=
